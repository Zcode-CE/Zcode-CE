# 上游同步台账

> **用途**：累积记录"我们与上游的关系"—— 每个上游版本同步了什么、有意未同步什么、以及**确认过不存在**什么。
> **与 release notes 的分工**：release notes 的「同步上游」一节面向用户、只写**本版**且**用户可见**的同步情况；
> 本台账是**跨版本累积**的完整记录（含内部项与依据），供维护者查"我们落后多少、为什么没跟"。
> **不要**把本台账的内容复制进 README —— README 的「与官方开源版的差异」一节已表达"除差异外一致"，再写会重复。

## 记录格式

| 字段         | 含义                                                                                        |
| ------------ | ------------------------------------------------------------------------------------------- |
| 条目         | 同步对象（能力/修复/协议）                                                                  |
| 上游版本     | 来自哪个上游版本                                                                            |
| 来源         | `开源仓库` / `闭源包`                                                                       |
| 依据         | `源码比对`（确信度高）/ `逆向分析`（确信度低，**有源码后应回头复核**）/ `官方 release note` |
| 同步情况     | `已跟进` / `未跟进（有意）` / `部分跟进` / `不适用`（**确认过：上游也不提供**）             |
| 我们何时落地 | 本仓库的版本号或"未落地"                                                                    |

**`不适用` 与 `未跟进（有意）` 的区别很重要**：前者是"查过、上游也没有"，后者是"上游有、我们决定不要"。两者都不能写成"已跟进"。

## 基线（会漂移的值，标快照时刻）

| 项                           | 值                                                    | 快照时刻   |
| ---------------------------- | ----------------------------------------------------- | ---------- |
| 我们的 fork 点（merge-base） | `872ad960`（`feat: open source`）                     | 2026-09-24 |
| 上游最新 tag                 | `v3.14.3` = `29628c9`                                 | 2026-09-24 |
| 我们领先 / 落后              | 领先 164 提交 / 落后 1 提交（上游把整版压成一个提交） | 2026-09-24 |

> 复算方式：`git fetch upstream --tags && git merge-base HEAD upstream/main && git rev-list --left-right --count HEAD...upstream/main`。
> **注意**：上游 `v3.14.3` 的**唯一父提交**就是开源提交 `872ad96`，因此"v3.14.3 新增"与"开源基线就有"可以精确切分 —— 比只看一棵树可靠。

## v3.14.3（来源：开源仓库）

| 条目                                                                  | 来源     | 依据     | 同步情况                                                                            | 我们何时落地                                                           |
| --------------------------------------------------------------------- | -------- | -------- | ----------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| 工作流引擎：续跑确定性修复（`replay-order`，ReplaySettleOrder）       | 开源仓库 | 源码比对 | 已跟进                                                                              | 待填（本批）                                                           |
| 工作流卡片：性能与稳定性建模（`runIndex` + 时间线缓存）               | 开源仓库 | 源码比对 | 已跟进                                                                              | 待填（本批）                                                           |
| 工作流失败语义（`wire-fault` + `fault.subscription.contentRejected`） | 开源仓库 | 源码比对 | 已跟进                                                                              | 待填（本批）                                                           |
| 协议 v4：workflowRuns 扩张（op 5→7、实例上限 256→1024、新增 9 文件）  | 开源仓库 | 源码比对 | **未跟进（有意）**                                                                  | 未落地 —— 必须整体搬运（抬界不配套裁剪会把"静默丢实例"升级成"整帧丢"） |
| IM 机器人（Bot Channel：27 文件 / 13830 行 + BotsDialog 2369 行）     | 开源仓库 | 源码比对 | **部分跟进** —— 入口与启用流程同步且默认关闭；服务端能力与遥测处理待定              | 待填（本批）                                                           |
| 上游把 bundled 技能从引用面板过滤掉                                   | 开源仓库 | 源码比对 | 待定                                                                                | 未落地（我方缺该 filter，composer `$` 面板会列出 `dynamic-workflows`） |
| headless `--enable-workflow` 开关                                     | 开源仓库 | 源码比对 | **部分跟进** —— 保留官方拼写，但默认值与官方相反（本仓库默认开启）                  | 已落地（本批）—— 另加 `--no-enable-workflow`，见 `official-diff.md`    |
| `/workflow` 的两处路由分支（CLI 命令中心）                            | 开源仓库 | 源码比对 | **部分跟进** —— 11+8 行已逐字节照搬；序 10 的另两处接线与 bundled-skills 解析器仍欠 | 已落地（本批，2026-09-24）—— 欠账见 FEATURE-DIFF 序 10                 |

### 确认过"上游也不提供"（`不适用`）

| 条目                    | 来源     | 依据                     | 结论                                                                                                                |
| ----------------------- | -------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------- |
| 云端 relay              | 开源仓库 | 源码比对（全仓穷尽检索） | **不存在** —— `relayWsUrl`/`relayUrl`/`relayServer`/`cloudRelay`/`wsRelay`/`mid=` 全部 0 命中；`wss://` 仅 5 处回环 |
| 配对服务器 / 设备配对码 | 开源仓库 | 源码比对                 | **不存在** —— 有"码"但全是 IM 身份注册码，非设备配对码                                                              |
| 移动壳 / PWA / 推送     | 开源仓库 | 源码比对                 | **不存在**                                                                                                          |

> 这三条是**防止后人重复怀疑**的记录：官方开源版没有中继、没有配对服务器、没有移动壳；本仓库同样不引入中继。

## 已修正的既有结论（逆向时代遗留）

| 结论                           | 原表述                                             | 实测                                                                                                                                                                                                   | 处置                    |
| ------------------------------ | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------- |
| 上游"整块移除"手机远控         | 根 `AGENTS.md`：官方开源前已把手机远控**整块移除** | **不成立**：`872ad96:packages/shared/src/task-realtime-core.ts:102-103` 已有 `relay_owner`/`relay_bridge` 枚举，`desktopRemoteSessions.ts` 也在基线里 ⇒ 准确说法是**移除产品面、保留协议与传输层接缝** | 已就地订正（`dd95e0c`） |
| 二维码"需引入新依赖、本版不做" | `docs/development/web-remote-control.md` 三处      | **不成立**：`qrcode@^1.5.4` 是从开源基线继承的**既有依赖**；面板已实现                                                                                                                                 | 已就地订正（`dd95e0c`） |

> 教训：**逆向时代的结论在有源码后必须回头复核**。本台账的「依据」一栏就是为了让这件事可追踪。
