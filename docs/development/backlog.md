# 欠账台账（Backlog）

> **用途**：唯一汇总处。任何"未做 / 排后续 / 有意不做"的东西都记在这里 —— 否则它会散落在 release-notes、分析报告、清单、issue 里，**然后被漏掉**。
> **建立原因**：2026-09-24 汇总本批欠账时发现它们散落在**四处**（release-notes 的已知限制、`.reverse/90-upstream-v3143/` 的建议序、`.reverse/45-release/CE3-CHECKLIST.md`、`.reverse/92-copy-format/`），**没有一处能看到全貌**。

## 记录格式

| 字段 | 含义                                                        |
| ---- | ----------------------------------------------------------- |
| 条目 | 要做什么（一句话）                                          |
| 类   | `正确性/安全` / `上游跟进` / `协议引擎` / `产品面` / `工程` |
| 来源 | 结论出自哪份报告或哪次决策                                  |
| 代价 | S / M / L                                                   |
| 风险 | 低 / 中 / 高（含"不做会怎样"）                              |
| 状态 | `待排` / `已排入 <版本>` / `有意不做（原因）` / `已完成`    |

**规则**：① **有意不做**必须写原因（否则后人会以为是漏了）；② 完成时**不要删行**，改成 `已完成` 并写落在哪个提交/版本；
③ 每批收尾时**回扫本表**（与"发布后同步四处"同级）。

---

## 已排入本版（ce.3，进行中）

| 条目                                                                                                     | 类          | 来源                                       | 代价 | 风险                                                | 状态                                |
| -------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------ | ---- | --------------------------------------------------- | ----------------------------------- |
| IM 机器人入站独立接入路线 spec（独立前缀 `/bot/**`、默认不注册、独立凭据 + 限流）                        | 正确性/安全 | 用户拍板（不走令牌面）                     | M    | 中（安全面设计）                                    | 已排入（spec 阶段，task-97）        |
| 技能门（C4）+ 工具描述瘦身（26.7K→4.5K）                                                                 | 正确性/安全 | FEATURE-DIFF §5.1 序 3；task-52 遗留不一致 | M    | **高**（模型不读技能则 `CreateWorkflow` 功能倒退）  | 已排入（task-98，**先实测再动手**） |
| desktop bundled-skills stage                                                                             | 正确性/安全 | FEATURE-DIFF §5.1 序 11（**必须做**）      | S    | 低（漏了桌面包 `/workflow` 展开成"技能文件不存在"） | 已排入（task-99）                   |
| 上游 S 档批量：卡片按钮溢出 / headless `--enable-workflow` / 草稿读状态 / 引擎结构纯化 / onboarding 收敛 | 上游跟进    | FEATURE-DIFF §5.1 序 2·4·5·6·12            | S    | 低                                                  | 已排入（task-100）                  |

## 待排（下一版候选）

### 产品面：远程控制的连接面（**用户已认可进下一版**）

| 条目                                          | 类     | 来源                                       | 代价 | 风险                              | 状态                     |
| --------------------------------------------- | ------ | ------------------------------------------ | ---- | --------------------------------- | ------------------------ |
| 已连设备列表 + 逐设备断开                     | 产品面 | 用户拍板                                   | M    | 中（需服务端 connections 数据源） | 待排（下一版）           |
| 端口与监听范围选择                            | 产品面 | 同上                                       | S    | 低                                | 待排（下一版）           |
| 令牌轮换入口（面板内）                        | 产品面 | 同上                                       | S    | 低                                | 待排（下一版）           |
| 入口的 `waiting`（等待连接）状态**加回来**    | 产品面 | 本批因无数据源而删（CE3-CHECKLIST 规矩 A） | S    | 低（依赖连接面）                  | 待排（下一版，随连接面） |
| 设备级令牌吊销（替代"换令牌 = 所有设备掉线"） | 产品面 | release-notes 已知限制                     | M    | 中（需设备维度状态所有者）        | 待排                     |

### 协议/引擎（**原子单元，不可拆**）

| 条目                                                                                    | 类       | 来源                                                              | 代价  | 风险                                                   | 状态                                                          |
| --------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------- | ----- | ------------------------------------------------------ | ------------------------------------------------------------- |
| **M4+M5+C1+C2+C3**：协议 v4 目录整体搬运 + 老消费者兜底 + 界 256→1024 + 腾位 + 表外计数 | 协议引擎 | PROTOCOL-DRIFT §5.1（**已核证前置条件**：v4 目录我方历史 0 改动） | **L** | **高**（抬界不配套裁剪会把"静默丢实例"升级成"整帧丢"） | 待排                                                          |
| **序 7/8/9**：协议 v4 workflowRuns 重写 / 并发就地调整 / 续跑在飞 ask                   | 协议引擎 | FEATURE-DIFF §5.1 序 7·8·9                                        | **L** | **最高**（线格式扩张 + 归约重写 + 转录边界）           | 待排（**必须自补黄金测试** —— 上游自己没带 protocol-v4 测试） |
| M1+M3：`display` 4 处 `.catch(undefined)` + `predecessorRunId` 可选化                   | 协议引擎 | PROTOCOL-DRIFT §5.1（**S，合计 5 行**）                           | S     | 极低                                                   | 待排（可随时插队）                                            |
| 序 10：`/workflow` 三处接线对齐 + bundled-skills 解析器（含 SEA）                       | 上游跟进 | FEATURE-DIFF §5.1 序 10                                           | M     | 中（**含 3 处真实冲突**）                              | 待排                                                          |

### 工程 / 文档

| 条目                                                                                                                                     | 类   | 来源                                                                                     | 代价 | 风险                                                                            | 状态                                       |
| ---------------------------------------------------------------------------------------------------------------------------------------- | ---- | ---------------------------------------------------------------------------------------- | ---- | ------------------------------------------------------------------------------- | ------------------------------------------ |
| 格式线第二批：4 份文档（按"读者做错决定的代价"排序）+ 注释铺开（约 3300 对）                                                             | 工程 | task-94 收口时确定                                                                       | M    | 低                                                                              | 待排                                       |
| `font-provisioning.md` 重复章节编号（三/四/五 各两次 + 一处结论矛盾）                                                                    | 工程 | task-94 发现（属改内容非格式）                                                           | S    | 中（"能让人做错决定"的文档缺陷）                                                | 待排                                       |
| 棘轮基线对齐到已提交状态（`scheduler-types.ts` / `scheduler.ts` 那两处未提交工作）                                                       | 工程 | task-94 收口风险                                                                         | S    | 低（风险方向安全：只会报"计数下降"）                                            | 待排（等那位提交后重跑 `--write`）         |
| 许可材料闭环（`--strict` 的 `reviewRequired`；条数取 `node -p "require('./third-party/inventory.json').reviewRequired.length"`，不写死） | 工程 | 用户批准的两档口径                                                                       | L    | 中（上游材料本身不全）                                                          | 待排（**已按两档口径书面声明**，非阻塞）   |
| musl 半支持（服务能跑、终端不可用 → 自建 musl node-pty + musl bfs/ugrep + 平台类）                                                       | 工程 | CE3-CHECKLIST（**已标触发条件**）                                                        | L    | 中                                                                              | 待排（**触发条件**：有真实 musl 用户诉求） |
| **npm 多包发布**（主包薄 + 平台/载荷分包作 `optionalDependencies`）                                                                      | 工程 | 用户 2026-09-25 拍板：**本版先单包打通，多包排后续**                                     | M    | 低（单包 66 MB 可用；多包是为"`npx` 更快 + 体积不贴限"）                        | 待排（下一版）                             |
| **重跑 `official-diff.md` 整篇对照到 3.14.3**                                                                                            | 工程 | task-115 J1（用户裁定：**先标注快照身份**，重跑另立）                                    | L    | 中（该文档是"某时刻的对照"，已标快照身份与否定式护栏，但逐条结论仍出自 3.14.1） | 待排                                       |
| **`node-repl-host/dist-types/` 构建产物被跟踪**（16 个 `tsc` 产出；`.gitignore` 漏了 `dist-types/` ⇒ 每次构建后被 `git add` 扫进）       | 工程 | 用户 2026-09-25 指出；Lead 实测：有专门提交 `28ab20e`「同步 dist-types（构建链再生成）」 | S    | 低（但会让每次构建都产生噪声 diff）                                             | 已排入本版                                 |
| **Docker 镜像发布**（`ghcr.io/<org>/zcode-ce-server`）                                                                                   | 工程 | 用户 2026-09-25：**不发布（麻烦）**；仓库根 `Dockerfile` + `compose.yaml` 本地构建可用   | M    | 低                                                                              | 有意不做（用户决定：维护与上传成本不值）   |
| **`packages/web` 端到端测试依赖本机登录态**（本机无 zai OAuth 凭据 ⇒ 稳定红；CI 上因 dist 不存在而 `skip`）                              | 工程 | 本批 task-100 发现 + Lead 核实（同一份代码换登录态即由红变绿）                           | M    | 中（**CI 上这 4 条的牙齿是拔掉的** ⇒ 验证盲区）                                 | 待排（下一版）                             |
| **onboarding 经 RPC 时 host 未注册 channel 会挂起而非 reject**（`catch` 抓不到；`appendRecord` 有 5s 超时，`dismissOnboarding` 没有）    | 工程 | 本批 task-108 未验证项                                                                   | S    | 中（挂起只影响"决策没落盘"，不阻塞 UI，但属静默）                               | 待排（下一版）                             |
| **web 端 `deviceMid` 与设备身份不同源**（web 用屏幕指纹，服务侧用 `ensureDeviceMid`）                                                    | 工程 | 本批 task-108 未验证项                                                                   | S    | 中（同一用户在不同入口可能得到不同 deviceMid）                                  | 待排（下一版）                             |

## 有意不做（**写清原因，防后人误以为漏了**）

| 条目                                                                                             | 类       | 原因                                                                | 来源                        |
| ------------------------------------------------------------------------------------------------ | -------- | ------------------------------------------------------------------- | --------------------------- |
| **云端 relay / 配对服务器 / 移动壳**                                                             | 产品面   | **实测上游开源版也不提供**（全仓检索 0 命中）；本项目同样不引入中继 | CLOSED-SOURCE-MECHANISMS §6 |
| **Bot Channel 的第三方渠道实现**（weixin / feishu / lark 之外的留位渠道 dingding/discord/wecom） | 上游跟进 | 上游也只留位、无实现证据                                            | TELEMETRY-AUDIT §8          |
| **README 的"更新至 v3.14.3"措辞**                                                                | 文档     | 那是官方仓库自己的更新日志，不是产品文档                            | FEATURE-DIFF §5.2           |

## 已知与上游的**有意偏离**（不是欠账，是决策）

| 项            | 我们的选择          | 上游                     | 原因                                                                                                       |
| ------------- | ------------------- | ------------------------ | ---------------------------------------------------------------------------------------------------------- |
| 飞书 SDK 版本 | **1.74.0**          | 1.64.0                   | 1.64.0 的两个缺陷正好落在飞书唯一使用的长连接路径上：一个**杀宿主进程**、一个**静默丢事件**（1.74.0 已修） |
| 手机远控形态  | **自托管 Web 控制** | IM 机器人（Bot Channel） | 不引入中继、不做 IM 机器人（两条路径以标签页共存，Web 控制默认开、Chat bot 默认关）                        |
